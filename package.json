{
  "name": "stripe-sample-checkout-with-multiple-locales",
  "version": "1.0.0",
  "description": "Stripe Checkout with multiple locals.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "stripe-samples",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.0.0",
    "express": "^4.19.2",
    "stripe": "^7.1.0"
  }
}
